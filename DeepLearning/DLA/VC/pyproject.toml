[tool.poetry]
name = "vc"
version = "0.1.0"
description = ""
authors = ["mset"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
ipykernel = "^6.27.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
